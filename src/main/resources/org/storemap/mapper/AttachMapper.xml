<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.storemap.mapper.AttachMapper">

	<insert id="insert" parameterType="org.storemap.domain.AttachFileVO">
		INSERT INTO attach_file (
            attach_idx,
            event_idx,
            store_idx,
            enter_idx,
            filename,
            member_idx
        ) VALUES (
            attach_seq.nextval,
            #{event_idx},
            #{store_idx},
            #{enter_idx},
            #{filename},
            #{member_idx}
        )
	</insert>
	<!-- 2. 파일 수정 -->
	<update id="update" parameterType="org.joonzis.domain.AttachFileVO">
		update attach_file set 
			event_idx = #{event_idx},
			store_idx = #{store_idx},
			enter_idx = #{enter_idx},
			fileName = #{fileName},
			member_idx = #{member_idx}
			where attach_idx = #{attach_idx}
	</update>
	<!-- 3. 파일 삭제 -->
	<delete id="delete" parameterType="int">
		delete from attach_file where attach_idx = #{attach_idx}
	</delete>
	<!-- 4. 파일 목록 -->
	<select id="findByIdx" parameterType="int" resultType="org.joonzis.domain.AttachFileVO">
		select * from attach_file where attach_idx = #{attach_idx}
	</select>
	
</mapper>